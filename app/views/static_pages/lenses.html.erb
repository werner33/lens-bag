<div class="crazy">
   <h1 class="learn">Learn About Lenses!</h1>
   <p class="summary">
       Going to Lengths To Explain Focal Lengths.
   </p>
</div>
<div class="container">
    <div class="lenses col-md-12">
        <div class="row">
            <div class="intro">
                <h3>Click Below To Learn More About Each Lens.</h3>
            </div>
            <h4 class="collapse">Collapse All</h4>
        </div>
        <% @lenses.each do |lens| %>
            <div class="title">
              <h3><%= lens.name %></h3>
            </div>
            
            <div class="description">
                <div class="lens_image col-md-3">
                   <img class="lens_image" src="assets/lens.jpg" />
                </div>
                <p>
                    <%= lens.description %>
                </p>
                <div class="row buttons">
                    <button class="btn btn-default btn-lg btn-primary">
                        <span class="glyphicon glyphicon-camera" aria-hidden="true"></span>
                        <%= link_to "Photo Gallery", lens.gallery_url, class: "gallery" %>
                    </button>
                    <button class="btn btn-default btn-lg btn-success">
                        <span class="glyphicon glyphicon-search" aria-hidden="true"></span>
                        <%= link_to "Shop for this Lens.", lens.shop_url, class: "shop" %>
                    </button>
                </div>
            </div>
        <% end %> 
    </div>
</div>

